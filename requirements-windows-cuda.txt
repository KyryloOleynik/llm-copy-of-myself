# Native Windows / NVIDIA CUDA 12.8 training environment.
# The PyTorch index supplies the CUDA wheel; PyPI supplies the remaining packages.
--index-url https://download.pytorch.org/whl/cu128
--extra-index-url https://pypi.org/simple

# Pin the CUDA-local version. Without +cu128, pip can prefer a newer CPU-only
# torch release from the PyPI extra index.
torch==2.11.0+cu128
accelerate>=1.2,<2
bitsandbytes>=0.45,<1
datasets>=3.2,<5
numpy>=1.26,<3
peft>=0.14,<1
safetensors>=0.5,<1
# Transformers 5.13.1 commit tested with Qwen3-4B-Instruct-2507 chat templates.
transformers @ git+https://github.com/huggingface/transformers.git@4626421dc6b741a329300682a6408246ee465490
trl>=0.15,<1

# Installs this repository and its YAML, configuration, and CLI dependencies.
-e .
